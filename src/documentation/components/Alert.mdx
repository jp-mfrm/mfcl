---
name: Alert
menu: Components
route: /components-alert
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import { Alert, Button } from '../../lib'

# Alert

```javascript
import Alert from 'mfcl/Alert'
```

Alerts are to assist communication between the user and the UI

## Properties

<Props of={Alert} />

## Colors

- You can pass in anything with the children prop

<Playground>
  <Alert color="error" style={{ width: '300px' }}>
    Error: you are receiving a crazy error
  </Alert>
</Playground>

## Controlled Alert

- Use the collapse or fade prop (or both!) for a smoother transition
- Or set them to false if you don't want an animated transition (for some reason..)

<Playground>
  <Component
    initialState={{
      isOpen1: true,
      isOpen2: true,
      isOpen3: true
    }}
  >
    {({ setState, state }) => (
      <>
        <Alert color="error" isOpen={state.isOpen1} onClose={() => setState({ isOpen1: false })}>
          This alert can be closed/controlled
        </Alert>
        <Alert color="error" isOpen={state.isOpen3} onClose={() => setState({ isOpen3: false })}>
          This alert has a fade and collapse animation
        </Alert>
        <Alert color="error" isOpen={state.isOpen2} onClose={() => setState({ isOpen2: false })} collapse={false}>
          This alert has a fade animation
        </Alert>
        <Button
          onClick={() =>
            setState({
              isOpen1: true,
              isOpen2: true,
              isOpen3: true
            })
          }
        >
          Refresh
        </Button>
      </>
    )}
  </Component>
</Playground>
