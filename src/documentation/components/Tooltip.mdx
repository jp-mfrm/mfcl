---
name: Tooltip
menu: Components
route: /components-tooltip
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import { Tooltip, Button, Select } from '../../lib'
import { Row } from './helpers'

# Tooltip

```javascript
import { Tooltip } from 'mfcl'
```

## Description

Tooltips help clarify what certain features do.

## Properties

<Props of={Tooltip} />

## Basic Usage

<Playground>
  {() => {
    const margin = { marginBottom: 5 }
    return (
      <Row>
        <Tooltip trigger={<Button style={margin}>Hover Me</Button>}>Now you see me!</Tooltip>
        <Tooltip hover={false} trigger={<Button style={margin}>Click Me</Button>} position="bottom">
          Now you see me!
        </Tooltip>
        <Tooltip arrow={false} trigger={<Button style={margin}>No arrow</Button>} position="bottom">
          Where did my arrow go?
        </Tooltip>
        <Tooltip arrow={false} hover={false} trigger={<Button style={margin}>No arrow Click</Button>} position="bottom">
          Where did my arrow go?
        </Tooltip>
        <Tooltip
          backgroundColor="#d63426"
          hover={false}
          trigger={<Button style={margin}>Background Color</Button>}
          position="bottom"
        >
          Cool Color Bruh
        </Tooltip>
      </Row>
    )
  }}
</Playground>

## Positions

<Playground>
  {() => {
    const positions = ['top', 'top-left', 'top-right', 'bottom', 'bottom-left', 'bottom-right', 'left', 'right']
    return (
      <Row>
        {positions.map((position) => (
          <Tooltip
            key={position}
            position={position}
            trigger={`I am on the ${position}`}
            style={{ margin: '0px 10px' }}
          >
            {position.toUpperCase()}
          </Tooltip>
        ))}
      </Row>
    )
  }}
</Playground>

## Animations

<Playground>
  <Component
    initialState={{
      offset: 0,
      delay: 0,
      duration: 180,
      easing: 'ease-in-out'
    }}
  >
    {({ setState, state }) => {
      const marginRight = { marginRight: 8 }
      return (
        <>
          <Row>
            <div>
              <label style={marginRight}>Offset</label>
              <input
                placeholder="Offset"
                name="offset"
                type="number"
                onChange={(e) => setState({ offset: +e.target.value })}
                value={state.offset}
              />
            </div>
            <div>
              <label style={marginRight}>Delay</label>
              <input
                placeholder="Delay"
                name="delay"
                type="number"
                onChange={(e) => setState({ delay: +e.target.value })}
                value={state.delay}
              />
            </div>
          </Row>
          <Row>
            <div>
              <label style={marginRight}>Duration</label>
              <input
                placeholder="Duration"
                name="duration"
                type="number"
                onChange={(e) => setState({ duration: +e.target.value })}
                value={state.duration}
              />
            </div>
            <Select
              label="Easing"
              placeholder="Easing"
              onChange={(e) => setState({ easing: e.target.value })}
              value={state.easing}
            >
              <option value="ease">ease</option>
              <option value="easing-in">easing-in</option>
              <option value="easing-out">easing-out</option>
              <option value="easing-in-out">easing-in-out</option>
              <option value="linear">linear</option>
              <option value="step-start">step-start</option>
              <option value="step-end">step-end</option>
            </Select>
          </Row>
          <div style={{ marginTop: '100px', textAlign: 'center' }}>
            <Tooltip
              trigger={<Button style={{ marginBottom: 5 }}>Hover Me</Button>}
              delay={state.delay}
              duration={state.duration}
              easing={state.easing}
              offset={state.offset}
            >
              Customize me by changing the inputs above
            </Tooltip>
          </div>
        </>
      )
    }}

  </Component>
</Playground>
