import React, { FunctionComponent } from 'react';

import styles from './{{camelCase componentName}}.module.scss';

interface Props {
  [rest: string]: unknown; // ...rest property
};

const {{componentName}}: FunctionComponent<Props> = ({
  ...rest
}) => {
  return (
    <div className={styles['{{dash-case componentName}}-wrapper']} {...rest}>
      {{componentName}}
    </div>
  );
};

export default {{componentName}};
