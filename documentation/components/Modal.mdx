---
name: Modal
menu: Components
route: /components-modal
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import mattressIpsum from 'mattress-ipsum'
import { Modal, Button } from '../../src'

# Modal

```javascript
import Modal from 'mfcl/Modal'
```

## Properties

<Props of={Modal} />

## Basic Usage

NOTE: DO NOT add "Component" as a wrapper. That is only for documentation. Use React state/context to manage open/close

<Playground>
  <Component initialState={{ isOpen: false }}>
    {({ setState, state }) => {
      return (
        <div>
          <Modal
            header="Basic Modal"
            subheader="Subheader Details"
            isOpen={state.isOpen}
            onClose={(isOpen) => setState({ isOpen: false })}
          >
            {mattressIpsum(23)}
            <br />
            <br />
            <Button onClick={() => setState({ isOpen: false })}>Close Modal</Button>
          </Modal>
          <Button btnType="primary" onClick={() => setState({ isOpen: !state.isOpen })} size="lg">
            Open Modal
          </Button>
        </div>
      )
    }}
  </Component>
</Playground>

## Custom Usage

<Playground>
  <Component initialState={{ isOpen: false }}>
    {({ setState, state }) => {
      return (
        <div>
          <Modal
            header={<div>Basic Modal (Element)</div>}
            subheader={<div style={{marginBottom: '15px'}}>Subheader Details (Element)</div>}
            isOpen={state.isOpen}
            borderStyle="square"
            onClose={(isOpen) => setState({ isOpen: false })}
          >
            {mattressIpsum(23)}
            <br />
            <br />
            <Button onClick={() => setState({ isOpen: false })}>Close Modal</Button>
          </Modal>
          <Button btnType="primary" onClick={() => setState({ isOpen: !state.isOpen })} size="lg">
            Open Modal
          </Button>
        </div>
      )
    }}
  </Component>
</Playground>
