---
name: Slider
menu: Components
route: /components-slider
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import { Slider } from '../../src'

# Slider

```javascript
import Slider from 'mfcl/Slider'
```

## Description

TODO add description here

## Properties

<Props of={Slider} />

## Basic Usage

<Playground>
  <Component initialState={{ value1: 0, value2: 2 }}>
    {({ setState, state }) => (
      <Slider
        onChange={(key, value) => {
          setState({ [key]: value })
        }}
        onChangeCommited={(key, value) => {
          console.log('Change commited. Send to API')
        }}
        labels={['$100', '$1000', '$2000', '$4000', '$8000']}
        value1={state.value1}
        value2={state.value2}
        max={4}
      />
    )}
  </Component>
</Playground>
