---
name: Tabs
menu: Components
route: /components-tabs
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import mattressIpsum from 'mattress-ipsum'
import { Tabs } from '../../src'
import tabData, { tabContent } from './helpers/TabData.tsx'

# Tabs

```javascript
import { Tabs } from 'mfcl'
```

## Description

Tabs allow you to switch between different views of related information.

## Properties

<Props of={Tabs} />

## Basic Usage

<Playground>
  <Tabs name="default" items={tabData} defaultValue={2} />
</Playground>

## Controlled or Uncontrolled

<Playground>
  <Component initialState={{ activeTab: 1 }}>
    {({ setState, state }) => (
      <Tabs
        name="controlled"
        onChange={(activeTab) => setState({ activeTab })}
        value={state.activeTab}
        items={tabData}
      />
    )}
  </Component>
</Playground>

## Custom Tabs

<Playground>
  <Component initialState={{ activeTab: 1 }}>
    {({ setState, state }) => (
      <Tabs
        name="controlled"
        onChange={(activeTab) => setState({ activeTab })}
        value={state.activeTab}
        items={tabData}
      />
    )}
  </Component>
</Playground>
