---
name: Fade
menu: Components
route: /components-fade
---

import { Playground, Props } from 'docz'
import Component from 'react-component-component'
import mattressIpsum from 'mattress-ipsum'
import { Row } from './helpers'
import { Fade, Button } from '../../src'

# Fade

Fade creates a smooth UI/UX for React

## Install

React transition group is needed for Fade

```bash
yarn add react-transition-group
```

```javascript
import Fade from 'mfcl/Fade'
```

## Properties

<Props of={Fade} />

## Basic usage

<Playground>
  <Component
    initialState={{
      in: true,
      duration: 150
    }}
  >
    {({ setState, state }) => {
      const toggle = () => setState({ in: !state.in })
      return (
        <>
          <Button size="md" onClick={toggle}>
            Click Me
          </Button>
          <Fade in={state.in} duration={state.duration}>
            {mattressIpsum()}
          </Fade>
        </>
      )
    }}
  </Component>
</Playground>

## Callbacks

<Playground>
  <Component
    initialState={{
      in: true,
      firstText: '',
      text: ''
    }}
  >
    {({ setState, state }) => {
      const toggle = () => setState({ in: !state.in })
      return (
        <>
          <p>{state.firstText}</p>
          <p>{state.text}</p>
          <Button size="md" onClick={toggle}>
            Click Me
          </Button>
          <Fade
            in={state.in}
            duration={1000}
            onEnter={() => setState({ firstText: 'Enter' })}
            onEntering={() => setState({ text: 'Entering!' })}
            onEntered={() => setState({ text: 'Entered!' })}
            onExit={() => setState({ firstText: 'Exit!' })}
            onExiting={() => setState({ text: 'Exiting!' })}
            onExited={() => setState({ text: 'Exited!' })}
          >
            {mattressIpsum()}
          </Fade>
        </>
      )
    }}
  </Component>
</Playground>
